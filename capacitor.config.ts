import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ailtariel.lizmoneynote',
  appName: 'Liz Money Note',
  webDir: 'dist'
};

export default config;
