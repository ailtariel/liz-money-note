services:
  app:
    image: vue-skeleton:local
    build:
      context: .
    env_file:
      - .env.example
    environment:
      NGINX_PORT: 8080
      API_UPSTREAM: http://host.docker.internal:8080
      AUTH_UPSTREAM: http://host.docker.internal:8080
    ports:
      - "8080:8080"
