# 产品增强

- [ ] 允许自定义新增流水成功音效。
- [ ] 合并账本。
- [ ] 从剪贴板文本创建流水。

# 可选能力

- [ ] 如果 JSON 备份不能满足需求，增加原始 SQLite `.db` 文件导出和恢复。

# 已完成

- [x] 账户改为安全删除：历史流水或周期事件引用时阻止删除，账本默认账户安全替换；兼容历史归档恢复，并美化账本、账户管理界面。
- [x] Android Launcher 声明、图标资源和 APK 打包已验证，用户确认 Android 可以正常运行并结束后续设备检查。
- [x] 新增流水按钮移到页面下方正中，与四项底部导航保持独立，并验证移动端布局和全屏编辑器入口。
- [x] 统计页在自动汇率换算区域显示双语口径提示，并随账户币种联动。
- [x] 为 Tag 增加 10 色预设色板，历史空颜色、手工新增、文本导入和预载生成使用确定性循环分配。
- [x] 归档不再代表当前 backlog 的早期 MVP 与全局样式计划，统一历史实施日志状态口径。
- [x] 流水列表使用稳定 Map 和单次聚合减少重复查找，增加 Loading 并保证并发筛选最后一次请求生效。
- [x] 将 README 和公开默认标识从 Vue Skeleton 更新为 Liz Money Note，并记录开发、数据库、构建和发布流程。
- [x] 审计并消除 `jeep-sqlite` crypto externalized 和 ECharts chunk size 构建告警，迁移到 Vite 8 `rolldownOptions`。
- [x] App 首次自动检测到新版本时弹出一次更新提示，保留 About 红点和手动更新入口。
- [x] 增加默认账本和上次打开账本设置，流水页使用默认账本，新流水按最近有效账户回退。
- [x] 导入、导出、删除账本和保存流水增加 Loading，并阻止重复操作。
- [x] 修复首次启动时迁移先于资源数据库导入的问题，并清理构建资源目录中的多余数据库。
- [x] 修复流水编辑：详情页进入编辑器，保存时同步更新流水、Tag 和账户余额。
- [x] 设置 > 数据中四项合并为导入、导出两组。
- [x] 统计页及账户选择后的币种联动。
- [x] 全局 snack queue 模块。
- [x] 使用 `theme.change()` 替换废弃的 Vuetify 主题切换方式。
- [x] 数据导入重复确认和流水修复。
- [x] GitHub CI/CD 和 Android APK Release。
- [x] 自动更新检查、About 页、更新红点和 Android APK 安装。
- [x] 全局确认对话框组件。
- [x] 清理缓存不删除数据库，并增加确认提示。
- [x] 删除账本功能。
- [x] 流水列表使用 `item-height`、`shallowRef`、数组替换和稳定 key。
- [x] 流水输入键盘和单屏界面改造。
- [x] 账本与账户关联、默认账户和切换联动。
